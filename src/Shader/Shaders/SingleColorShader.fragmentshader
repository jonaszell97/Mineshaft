
#version 330 core

// Ouput data
out vec4 color;

// Values that stay constant for the whole mesh.
uniform vec4 singleColor;

void main()
{
//   float threshold = 0.01;
//   if (singleColor.a < threshold)
//      discard;

   color = singleColor;
}